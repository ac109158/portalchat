angular.module('portalchat.core').
service('ChatManager', ['$log', '$timeout', 'CoreConfig', 'UserManager','ChatBuilder',
function($log, $timeout, CoreConfig, UserManager,ChatBuilder) {
    var that = this;

    return this;
}]);
